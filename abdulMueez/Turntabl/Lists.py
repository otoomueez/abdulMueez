list = [] # an empty list
list.append("a") # this appends or adds figures to the empty list
print(list) # prints the list with the figures

def minmax():
    l = [1,2,5,3,2,6,6,4]
    m = min(l)
    n = max(l)
    